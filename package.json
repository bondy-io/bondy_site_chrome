{
  "name": "@bondy/site-chrome",
  "version": "0.3.0",
  "description": "Shared chrome for the Bondy site: the navigation bar, footer, palette and type that every Bondy property wears, so the independently-deployed sites read as one.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./src/index.js",
    "./sitemap": "./src/sitemap.js",
    "./styles/chrome.css": "./src/styles/chrome.css",
    "./assets/*": "./assets/*"
  },
  "files": [
    "src",
    "assets"
  ],
  "peerDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.4.0"
  }
}
